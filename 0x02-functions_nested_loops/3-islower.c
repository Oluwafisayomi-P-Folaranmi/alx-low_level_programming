#include "main.h"

/**
 * _islower -> checks if the character is lowercase.
 * main - check the code.
 * Return: returns 1 if c is lowercase, 0 otherwise.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
